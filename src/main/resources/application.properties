# spring.application.name=demoJDBC

# # MySQL config
# spring.datasource.url=jdbc:mysql://localhost:3306/teashop_java_db
# spring.datasource.username=root
# spring.datasource.password=
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # ❌ KHÔNG DÙNG JPA
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# ===============================
# APPLICATION
# ===============================
spring.application.name=demoJDBC
server.port=8080

# ===============================
# TARGET DATABASE
# POSTGRESQL - SUPABASE (ĐÍCH)
# ===============================
spring.datasource.url=jdbc:postgresql://db.doehsbuipqkmwgahcysb.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Teashop@2025!
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# SOURCE DATABASE
# MYSQL - LOCAL (NGUỒN)
# ===============================
mysql.datasource.url=jdbc:mysql://localhost:3306/teashop_java_db?useSSL=false&serverTimezone=UTC
mysql.datasource.username=root
mysql.datasource.password=
mysql.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JDBC CONFIG (KHÔNG DÙNG JPA)
# ===============================
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# ===============================
# CONNECTION POOL - POSTGRES
# ===============================
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# ===============================
# LOG JDBC (DEBUG KHI TEST)
# ===============================
logging.level.org.springframework.jdbc.core=DEBUG
logging.level.com.zaxxer.hikari=INFO
logging.level.org.springframework.jdbc.datasource=DEBUG
